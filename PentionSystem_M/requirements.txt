fastapi==0.110.0
uvicorn[standard]==0.30.1
requests==2.31.0

# --- OSM/DATI GEOGRAFICI ---
osmnx==1.9.1           # la più stabile per Docker
networkx==3.1          # compatibile con OSMNX 1.9.1
numpy==1.26.4
pandas==2.2.1
shapely==2.0.2         # compatibile con GEOS
geopandas==0.14.3      # stabile per GDAL/GEOS
rtree==1.1.0           # richiede libspatialindex (installato via apt)

# --- UTILITY ---
python-dotenv==1.0.1
