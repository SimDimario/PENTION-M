% !TEX root = ../main.tex

\chapter{Architettura e Realizzazione di \textsc{PENTION-M}}
\label{chap:architecture}

\section{Introduzione al Capitolo}

Il presente capitolo costituisce il nucleo tecnico della tesi e descrive in dettaglio l’architettura e la realizzazione del sistema \textsc{PENTION-M}, l’artefatto sviluppato nell’ambito del processo di \emph{Design Science Research} illustrato nel Capitolo~\ref{chap:dsr}.

In coerenza con l’approccio metodologico adottato, l’architettura di \textsc{PENTION-M} non viene presentata come una semplice implementazione software, ma come la concretizzazione tecnica dei requisiti emersi dalla campagna di \emph{Requirements Engineering} e delle iterazioni progettuali descritte nel capitolo precedente.
Ogni scelta architetturale, modellistica e infrastrutturale è pertanto riconducibile a specifiche esigenze operative, fisiche e forensi espresse dagli stakeholder del progetto.

Il sistema è concepito come un \emph{cyber-physical system} simulato che riproduce il funzionamento di un laboratorio mobile per il rilevamento e l’analisi di sostanze psicoattive emergenti.
Sebbene l’artefatto operi in un contesto simulato, l’architettura è stata progettata per riflettere fedelmente un possibile \textit{deployment} reale.
A tal fine, il sistema adotta un’architettura a livelli funzionali, pensata per separare in modo esplicito le fasi di acquisizione dei dati, modellazione fisica, inferenza, monitoraggio operativo e produzione di evidenze verificabili.

L’architettura complessiva integra:
(i) una pipeline \textit{end-to-end} per l’acquisizione e l’elaborazione dei dati;
(ii) modelli fisici e modelli di \emph{Physics-Informed Machine Learning};
(iii) una pipeline MLOps completa per il controllo del ciclo di vita dei modelli;
(iv) meccanismi di logging forense e generazione di evidenze digitali auditabili.

Il capitolo è organizzato come segue.
Nella Sezione~\ref{sec:arch_overview} viene presentata la visione architetturale complessiva di \textsc{PENTION-M}, introducendo i principi di design e la scomposizione del sistema in livelli architetturali funzionali.
Le sezioni successive approfondiscono il flusso operativo del sistema, i singoli layer fisici e di apprendimento automatico, la pipeline MLOps, il sistema di logging forense e l’interfaccia operativa.
Il capitolo si conclude con una discussione delle scelte architetturali e dei principali \textit{trade-off}, preparando il terreno per la valutazione dell’artefatto nel Capitolo~\ref{chap:evaluation}.

\section{Visione architetturale complessiva di \textsc{PENTION-M}}
\label{sec:arch_overview}

Dal punto di vista funzionale, \textsc{PENTION-M} implementa un flusso operativo \textit{end-to-end}, la cui realizzazione è supportata da una specifica architettura a layer che riproduce il comportamento di un laboratorio mobile per la \textit{detection} e l’analisi di sostanze psicoattive emergenti in contesto urbano.
Il sistema integra componenti di simulazione fisica, modelli di apprendimento automatico, servizi MLOps e meccanismi di logging forense all’interno di una pipeline coerente, tracciabile e riproducibile.

L’integrazione tra modelli fisici e apprendimento automatico si colloca nel filone del \emph{Physics-Informed Machine Learning}, ampiamente discusso in letteratura per sistemi fisici complessi, inclusi domini atmosferici e ambientali~\cite{kashinath2021piml}.

Dal punto di vista concettuale, \textsc{PENTION-M} è concepito come un \emph{cyber-physical system} simulato, in cui una componente fisica virtuale (rappresentazione dell’ambiente urbano, della dispersione chimica e del movimento del veicolo) è strettamente integrata con una componente computazionale responsabile dell’elaborazione dei dati, dell’inferenza e della produzione degli output operativi.
Questa integrazione consente di analizzare il comportamento del sistema in scenari realistici mantenendo il controllo sulla \emph{ground truth} e sui parametri ambientali.

L’intero sistema è implementato come insieme di servizi containerizzati, ciascuno responsabile di una specifica funzione della pipeline.
La containerizzazione~\cite{merkel2014docker} garantisce isolamento delle dipendenze, portabilità del sistema e riproducibilità delle esecuzioni, consentendo di simulare un \textit{deployment} operativo in cui i vari componenti cooperano come microservizi.
Nel contesto della presente tesi, tali servizi sono eseguiti in un ambiente computazionale controllato; tuttavia, l’architettura è progettata per riflettere uno scenario \emph{edge-only} e \emph{offline-capable}, in cui tutte le operazioni di inferenza, monitoraggio e logging avvengono localmente a bordo del laboratorio mobile.

Operativamente, \textsc{PENTION-M} implementa una pipeline sequenziale che inizia con la generazione e acquisizione dei dati di scenario, prosegue con l’elaborazione fisica e \textit{data-driven} delle informazioni, e si conclude con la produzione di output operativi e di evidenze forensi verificabili.
Ogni fase della pipeline è progettata per produrre metadati espliciti che descrivono lo stato del sistema, i parametri utilizzati e le versioni degli artefatti coinvolti, abilitando una tracciabilità \textit{end-to-end} del processo decisionale.

La visione architetturale adottata non mira a massimizzare le prestazioni di singoli modelli isolati, ma a garantire coerenza, robustezza e auditabilità dell’intero sistema.
In particolare, l’architettura privilegia:
(i) la separazione delle responsabilità tra le diverse fasi del flusso operativo;
(ii) l’integrazione esplicita di conoscenza fisica nei modelli di apprendimento;
(iii) il monitoraggio continuo del comportamento dei modelli in esecuzione;
(iv) la produzione nativa di evidenze digitali forensi.

Questa impostazione è coerente con le principali tassonomie del PIML, che distinguono modalità di integrazione della fisica a livello di feature, architettura e funzione obiettivo~\cite{meng2025piml}.

Sulla base di questi principi, l’architettura di \textsc{PENTION-M} è organizzata in un insieme di livelli architetturali funzionali, ciascuno dei quali incapsula una specifica porzione del flusso operativo.
La Sezione~\ref{sec:arch_layers} introduce formalmente tale architettura a layer, descrivendo il ruolo di ciascun livello e la loro interazione all’interno della pipeline \textit{end-to-end}.

\section{Architettura a 8 Layer di \textsc{PENTION-M}}
\label{sec:arch_layers}

L’architettura di \textsc{PENTION-M} è organizzata secondo un modello a \textbf{otto livelli architetturali funzionali}, numerati da Layer~0 a Layer~7, che descrive in modo esplicito la progressione del flusso operativo dal contesto fisico simulato fino alla produzione di output operativi e di evidenze forensi verificabili.

L’adozione di un’architettura a layer risponde a tre obiettivi principali:
(i) strutturare il sistema come una pipeline \textit{end-to-end} coerente;
(ii) separare responsabilità fisiche, computazionali e operative;
(iii) rendere tracciabili e auditabili le trasformazioni dei dati lungo l’intero processo decisionale.
Ogni layer incapsula una fase specifica del flusso e comunica con i livelli adiacenti tramite interfacce ben definite, riducendo accoppiamenti impliciti e facilitando l’analisi e l’evoluzione dell’artefatto.

La Figura~\ref{fig:pention_architecture_layers} mostra la visione complessiva dell’architettura a 8 layer di \textsc{PENTION-M}, evidenziando la suddivisione in tre macro-aree funzionali: \emph{Data Flow}, \emph{Inference} e \emph{Output}.

\begin{figure}[H]
	\centering
	\includegraphics[width=\textwidth]{img/PENTION-M_Architecture.png}
	\caption{Architettura a 8 layer di \textsc{PENTION-M} e flusso end-to-end del sistema.}
	\label{fig:pention_architecture_layers}
\end{figure}

\subsection*{Panoramica dei layer}

\begin{itemize}
	\item \textbf{Layer 0 – Edge I/O (Simulato)}:
	      rappresenta il punto di ingresso del sistema e simula la generazione dei segnali provenienti dai sensori virtuali, includendo condizioni meteorologiche, segnali di posizione e dati relativi alla sostanza rilasciata.

	\item \textbf{Layer 1 – Data Ingestion}:
	      gestisce la raccolta dei dati simulati, i controlli di validità e la loro normalizzazione in un formato strutturato coerente, che costituisce l’evento di riferimento per la pipeline.

	\item \textbf{Layer 2 – PIML Feature Layer}:
	      trasforma i dati grezzi in \textit{feature} fisicamente significative, integrando parametri ambientali, vincoli fisici e informazioni di contesto utili ai modelli di inferenza.

	\item \textbf{Layer 3 – Inference Services}:
	      comprende i servizi responsabili della predizione della dispersione, della localizzazione della sorgente e della classificazione della sostanza, combinando modelli fisici e modelli di \emph{Physics-Informed Machine Learning}.

	\item \textbf{Layer 4 – Monitoring \& Telemetry}:
	      osserva il comportamento dei modelli in esecuzione, raccogliendo metriche di latenza, stabilità e affidabilità utili al controllo operativo del sistema.

	\item \textbf{Layer 5 – ModelOps \& Logging}:
	      gestisce il versionamento degli artefatti, il tracciamento delle configurazioni di esecuzione e la produzione di \textit{audit trail} forensi.

	\item \textbf{Layer 6 – Interfaccia Utente Operativa}:
	      fornisce all’operatore una rappresentazione sintetica e interpretabile dei risultati, supportando il processo decisionale in tempo reale.

	\item \textbf{Layer 7 – Forensic Export}:
	      consente l’esportazione delle evidenze generate dal sistema sotto forma di pacchetti verificabili, conformi ai requisiti normativi e forensi.
\end{itemize}

\section{Flusso operativo end-to-end (vista runtime)}
\label{sec:runtime_flow}

Sebbene l’architettura di \textsc{PENTION-M} sia descritta attraverso una scomposizione a layer funzionali, il comportamento del sistema a \textit{runtime} può essere compreso più efficacemente adottando una vista trasversale \emph{end-to-end}.
Questa sezione descrive il flusso operativo della pipeline durante l’esecuzione, evidenziando la sequenza di trasformazioni che i dati subiscono dall’acquisizione iniziale fino alla produzione delle evidenze forensi esportabili.

Dal punto di vista logico, il sistema implementa un \textit{workflow} che attraversa più layer architetturali e può essere suddiviso nelle seguenti macro-fasi:

\begin{enumerate}
	\item \textbf{Patrolling e detection}: simulazione del movimento del laboratorio mobile e verifica delle condizioni di attivazione della pipeline;
	\item \textbf{Ingestion e normalizzazione}: ricezione del \textit{payload} sensoriale, validazione e costruzione dell’evento unificato;
	\item \textbf{Modellazione fisica e correzione PIML}: generazione delle mappe di dispersione e loro raffinamento fisicamente informato;
	\item \textbf{Inferenza e supporto decisionale}: localizzazione della sorgente e classificazione della sostanza;
	\item \textbf{Monitoring e ModelOps}: raccolta delle metriche \textit{runtime} e tracciamento delle versioni di modello;
	\item \textbf{Forensic export}: generazione del \textit{bundle} forense e del report operativo.
\end{enumerate}

Questa vista \textit{runtime} consente di separare la logica operativa del sistema dalla sua organizzazione architetturale, chiarendo come i diversi layer cooperino nella realizzazione di un processo decisionale tracciabile e riproducibile.
Nei paragrafi successivi, ciascun layer viene analizzato nel dettaglio, esplicitando il proprio contributo a una o più fasi del flusso \textit{end-to-end}.

\section{Layer 0 – Edge I/O e simulazione dei sensori}

Il Layer~0 rappresenta il punto di ingresso dell’intero sistema \textsc{PENTION-M} ed è responsabile della simulazione dei sensori e della generazione dei dati fisici e chimici che alimentano la pipeline \textit{end-to-end}.
In coerenza con la natura sperimentale dell’artefatto, tutti i dati acquisiti in questo layer sono interamente simulati, pur essendo progettati per riflettere in modo realistico il comportamento di un laboratorio mobile operativo.

Il layer implementa una simulazione integrata di sensori ambientali, di posizione e di rilevamento chimico, generando un evento di acquisizione strutturato che costituisce l’input per i layer successivi.
La scelta di concentrare la simulazione dei sensori in un layer dedicato consente di separare nettamente la generazione dei dati dalla loro elaborazione e inferenza, facilitando la tracciabilità e la riproducibilità degli esperimenti.

\subsection*{Sensori simulati}

Il sistema simula tre principali categorie di sensori:

\begin{itemize}
	\item \textbf{Sensori meteorologici}: generano parametri ambientali quali velocità e direzione del vento, temperatura, umidità relativa e classe di stabilità atmosferica secondo la classificazione di Pasquill--Gifford.
	      Tali parametri costituiscono una componente fondamentale per la modellazione della dispersione chimica nei layer successivi.

	\item \textbf{Sensori di posizione (GPS)}: simulano la posizione del laboratorio mobile all’interno di una \textit{bounding box} urbana predefinita.
	      Le coordinate geografiche sono generate in modo coerente con lo scenario urbano simulato, consentendo di associare ogni misura a un contesto spaziale realistico.

	\item \textbf{Sensori di sostanza}: simulano la rilevazione di concentrazioni chimiche e la generazione dello spettro di massa a ionizzazione elettronica (EI).
	      La sostanza da rilevare è selezionata in modo casuale da un dataset reale di spettri di massa di sostanze psicoattive emergenti, al quale viene applicato rumore controllato per simulare incertezze strumentali e condizioni operative realistiche.
\end{itemize}

La generazione dello spettro EI non si limita a una forma puramente sintetica, ma sfrutta spettri reali come base, garantendo una maggiore aderenza alle caratteristiche chimiche delle sostanze simulate e rendendo più significativo l’addestramento e la valutazione dei modelli di classificazione.

\subsection*{Sensore mobile e punto di campionamento}

Nel prototipo \textsc{PENTION-M} non viene simulata una rete di sensori distribuiti.
Tutte le misurazioni ambientali e chimiche sono acquisite a bordo del laboratorio mobile simulato.

Di conseguenza, il punto di campionamento coincide in ogni istante con la posizione del veicolo lungo la traiettoria di pattugliamento.
Le osservazioni disponibili al sistema assumono quindi la forma di una serie temporale di misure spazialmente correlate, la cui copertura dipende dal percorso effettuato dal veicolo nello scenario urbano.

Questa scelta progettuale riflette uno scenario operativo realistico e influenza direttamente la formulazione dei task di localizzazione e inferenza, che devono operare in condizioni di osservabilità parziale e dinamica.

\subsection*{Generazione dello scenario e casualità controllata}

Per ogni simulazione, il Layer~0 genera uno scenario indipendente identificato da un \texttt{simulation\_id} univoco e da un \textit{timestamp}.
La sostanza rilasciata, i parametri ambientali e la posizione del veicolo sono selezionati in modo pseudo-casuale.

Questo approccio consente di esplorare un ampio spazio di scenari operativi, mantenendo al contempo il controllo sulla \emph{ground truth} e sui parametri di simulazione, elemento essenziale per la successiva validazione dei modelli fisici e di \emph{Physics-Informed Machine Learning}.

\subsection*{Output del Layer 0}

L’output del Layer~0 consiste in un evento strutturato che aggrega tutte le misure simulate dei sensori e i relativi metadati.
Tale evento include esclusivamente dati grezzi e informazioni di contesto (timestamp, identificativo della simulazione, configurazione dello scenario), senza effettuare alcuna inferenza o decisione automatica.

L’evento generato viene trasmesso al Layer~1, dove ha inizio la fase di \textit{ingestion} e validazione dei dati.
Questa separazione garantisce che il Layer~0 rimanga limitato alla sola simulazione dell’acquisizione fisica, riflettendo il ruolo dei sensori reali in un sistema operativo e preservando la modularità complessiva dell’architettura di \textsc{PENTION-M}.

\section{Layer 1 -- Data Ingestion e validazione}
\label{sec:layer_ingestion}

Il Layer~1 rappresenta il punto di ingresso operativo della pipeline di \textsc{PENTION-M} e svolge la funzione di \emph{orchestratore centrale} del flusso \textit{end-to-end}.
A questo livello, i dati generati dal Layer~0 vengono ricevuti, validati, arricchiti e trasformati in un evento strutturato unificato che costituisce l’unità informativa di riferimento per tutte le fasi successive della pipeline.

Il servizio di \textit{ingestion} è implementato come microservizio containerizzato ed è progettato per operare in modalità \emph{edge-only} e \emph{offline-capable}, riflettendo uno scenario operativo reale in cui l’intero processo di acquisizione e analisi avviene localmente a bordo del laboratorio mobile.

\subsection{Struttura del payload e validazione iniziale}

Il Layer~1 riceve in ingresso un \textit{payload} JSON generato dal sistema di simulazione dei sensori virtuali.
Tale payload include informazioni eterogenee relative a:
(i) condizioni ambientali e meteorologiche;
(ii) posizione geografica del veicolo;
(iii) segnali chimici e spettrali associati alla sostanza rilasciata;
(iv) identificativi di simulazione e timestamp.

All’atto della ricezione, il payload è sottoposto a una fase di validazione sintattica e semantica.
La validazione include:
\begin{itemize}
	\item verifica della presenza dei campi obbligatori;
	\item controllo della coerenza temporale (timestamp in formato ISO~8601);
	\item normalizzazione delle coordinate geografiche rispetto al \textit{bounding box} di scenario;
	\item controllo dei range fisici dei parametri ambientali principali.
\end{itemize}

I dati validati vengono quindi normalizzati in una rappresentazione strutturata che costituisce l’\emph{evento unificato} della pipeline.

\subsection{Costruzione dell’evento unificato}

L’evento unificato aggrega in un singolo oggetto informativo tutte le informazioni rilevanti per l’inferenza, il monitoraggio e la tracciabilità forense.
In particolare, l’evento include:
\begin{itemize}
	\item dati grezzi dei sensori virtuali;
	\item \textit{feature} fisiche derivate (ad esempio parametri di dispersione);
	\item risultati intermedi e finali dei servizi di inferenza;
	\item metriche di monitoraggio \textit{runtime};
	\item metadati ModelOps relativi a versioni e configurazioni;
	\item metadati forensi e di compliance.
\end{itemize}

Questa scelta progettuale consente di mantenere una rappresentazione coerente e auto-consistente dello stato del sistema in ogni istante della pipeline, facilitando sia l’analisi operativa sia le attività di audit e validazione successive.

\subsection{Orchestrazione dei servizi downstream}

Una volta costruito l’evento unificato, il Layer~1 coordina l’invocazione dei servizi di inferenza \textit{downstream}.
In particolare:
\begin{itemize}
	\item il servizio di simulazione fisica e correzione PIML della dispersione;
	\item il servizio di localizzazione della sorgente basato su PIML;
	\item il servizio di classificazione della sostanza tramite modello XGBoost.
\end{itemize}

Il Layer~1 non implementa logica di inferenza diretta, ma funge da punto di integrazione tra i diversi moduli specializzati, gestendo la propagazione dei risultati all’interno dell’evento unificato.

\subsection{Integrazione con Monitoring e ModelOps}

Contestualmente all’orchestrazione dei servizi di inferenza, il Layer~1 inizializza e aggiorna i meccanismi di monitoraggio \textit{runtime}.
Vengono raccolte metriche relative a:
\begin{itemize}
	\item latenza dei servizi;
	\item stabilità delle predizioni;
	\item indicatori di \textit{drift} rispetto a baseline di riferimento;
	\item stato delle versioni di modello attive.
\end{itemize}

Le informazioni ModelOps associate all’evento includono l’identificativo del modello utilizzato, la versione del dataset di training e l’eventuale attivazione di \textit{trigger} di retraining.
Tali informazioni sono persistite in log strutturati e contribuiscono alla tracciabilità completa del ciclo di vita dei modelli.

\subsection{Logging forense e produzione delle evidenze}

La fase finale del Layer~1 prevede la generazione delle evidenze forensi.
L’evento unificato viene serializzato in forma canonica e sottoposto a hashing crittografico tramite algoritmo SHA--256~\cite{rfc6234_sha}.
Il \textit{digest} risultante viene firmato digitalmente mediante chiavi Ed25519~\cite{rfc8032_eddsa} generate e gestite localmente dal sistema.

Il risultato è un \emph{forensic bundle} contenente:
\begin{itemize}
	\item l’evento completo;
	\item l’hash crittografico;
	\item la firma digitale;
	\item la chiave pubblica di verifica;
	\item metadati di compliance e integrità degli artefatti.
\end{itemize}

I \textit{bundle} forensi sono immutabili e verificabili ex-post, consentendo la riproduzione e la validazione indipendente del processo decisionale del sistema.
In questo modo, il Layer~1 garantisce non solo l’integrità del flusso operativo, ma anche la conformità ai requisiti forensi e normativi definiti in fase di progettazione.

\section{Layer 2 -- Feature fisiche e PIML Feature Layer}
\label{sec:layer2_features}

Il Layer~2 rappresenta il livello di \emph{feature engineering} fisicamente informato di \textsc{PENTION-M}.
A partire dall’evento unificato prodotto dal Layer~1, questo livello ha l’obiettivo di:
(i) rendere espliciti i parametri fisici latenti rilevanti per la dispersione e la localizzazione della sorgente;
(ii) costruire rappresentazioni numeriche compatibili con i modelli di inferenza impiegati nei layer successivi;
(iii) integrare informazione contestuale urbana mediante maschere geometriche.

Nel prototipo implementato, una parte delle \textit{feature} fisiche viene materializzata già in fase di simulazione (\texttt{SensorSim\_M.py}) per esigenze di efficienza e semplificazione del flusso \textit{end-to-end}.
Tali feature vengono inserite nel campo \texttt{piml\_features} dell’evento e propagate ai servizi di inferenza (Layer~3) tramite \textit{payload} strutturati.
Dal punto di vista architetturale, questa scelta non altera il ruolo del Layer~2: le trasformazioni restano concettualmente riconducibili al livello di \textit{feature} fisiche, anche quando sono materializzate anticipatamente dal simulatore.

\subsection{Feature meteorologiche e parametri fisici di dispersione}

Il sistema include un insieme di parametri ambientali e fisici associati alla dispersione in atmosfera, tra cui condizioni meteorologiche, informazioni sul vento e variabili di stabilità.
In particolare, nel simulatore vengono prodotti i parametri:
\texttt{wind\_speed}, \texttt{wind\_dir} e \texttt{stability\_class}, che descrivono lo stato atmosferico dello scenario.

Per rendere la direzione del vento utilizzabile in modelli \textit{data-driven} e PIML, essa viene codificata anche tramite una rappresentazione vettoriale nel piano:
\begin{equation}
	\mathbf{v} =
	\begin{bmatrix}
		v_x \\ v_y
	\end{bmatrix}
	=
	\begin{bmatrix}
		\cos(\theta) \\ \sin(\theta)
	\end{bmatrix}
	\cdot \|\mathbf{v}\|,
\end{equation}
dove $\theta$ rappresenta la direzione del vento e $\|\mathbf{v}\|$ la sua intensità.
Questa codifica evita discontinuità angolari e migliora la stabilità dell’apprendimento.

Sulla base della classe di stabilità e delle condizioni di scenario, vengono inoltre calcolati i parametri di dispersione laterale e verticale $(\sigma_y, \sigma_z)$, che costituiscono parametri fisici espliciti del modello di dispersione gaussiano.
Tali quantità vengono propagate come \textit{feature} interpretabili e direttamente collegate alla fisica del fenomeno.

La Tabella~\ref{tab:piml_dataset_features} riassume in modo concettuale la struttura del dataset PIML utilizzato per l’addestramento e la validazione dei modelli fisicamente informati, evidenziando l’origine e il ruolo di ciascuna categoria di \textit{feature} all’interno dell’architettura.

\begin{table}[H]
	\centering
	\footnotesize
	\setlength{\tabcolsep}{5pt}
	\caption{Struttura concettuale del dataset PIML e origine delle \textit{feature} utilizzate nei modelli di inferenza.}
	\label{tab:piml_dataset_features}
	\begin{tabular}{p{2.4cm} p{3.2cm} p{5.2cm} p{2.4cm}}
		\toprule
		\textbf{Categoria}                                              & \textbf{Feature} & \textbf{Descrizione} & \textbf{Origine} \\
		\midrule
		Meteorologia                                                    &
		\texttt{wind\_speed}                                            &
		Velocità del vento nello scenario simulato                      &
		Simulatore (Layer~0)                                                                                                         \\

		Meteorologia                                                    &
		\texttt{wind\_dir}, $\cos(\theta)$, $\sin(\theta)$              &
		Direzione del vento e codifica vettoriale continua              &
		Layer~2                                                                                                                      \\

		Stabilità atmosferica                                           &
		\texttt{stability\_class}, \texttt{stability\_index}            &
		Classe di stabilità secondo Pasquill--Gifford e indice numerico &
		Simulatore / Layer~2                                                                                                         \\

		Ambiente                                                        &
		\texttt{RH}, \texttt{humidify}                                  &
		Umidità relativa e flag di umidificazione                       &
		Simulatore                                                                                                                   \\

		Sorgente                                                        &
		\texttt{source\_x}, \texttt{source\_y}, \texttt{source\_h}      &
		Coordinate e altezza della sorgente di emissione                &
		Scenario simulato                                                                                                            \\

		Dispersione                                                     &
		\texttt{emission\_rate}, $(\sigma_y, \sigma_z)$                 &
		Intensità di rilascio e parametri fisici di dispersione         &
		Modello fisico                                                                                                               \\

		Spazio                                                          &
		\texttt{gps\_x}, \texttt{gps\_y}                                &
		Coordinate normalizzate del laboratorio mobile                  &
		Layer~0                                                                                                                      \\

		Target                                                          &
		\texttt{concentration\_map}                                     &
		Campo di concentrazione di riferimento (ground truth simulata)  &
		Simulazione fisica                                                                                                           \\

		Contesto urbano                                                 &
		\texttt{building\_map}                                          &
		Maschera binaria degli edifici nell’area di interesse           &
		Preprocessing urbano                                                                                                         \\
		\bottomrule
	\end{tabular}
\end{table}

La distribuzione statistica delle principali variabili fisiche utilizzate nel dataset PIML è mostrata in Figura~\ref{fig:piml_physical_distributions}, a conferma della coerenza degli intervalli di simulazione con condizioni atmosferiche realistiche.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{img/distribuzioni_variabili_continue.png}
	\caption{Distribuzione delle principali variabili fisiche utilizzate nel dataset PIML, incluse velocità del vento, indice di stabilità atmosferica e umidità relativa. Gli intervalli mostrati riflettono condizioni atmosferiche realistiche coerenti con lo scenario simulato.}
	\label{fig:piml_physical_distributions}
\end{figure}

\subsection{Maschere urbane e contesto geometrico (building map)}

Oltre ai parametri atmosferici, \textsc{PENTION-M} integra informazione di contesto urbano attraverso una \emph{building map} binaria che approssima la presenza di edifici nell’area di interesse.
La maschera viene generata mediante lo script \texttt{binary\_map\_gen.py}, che interroga OpenStreetMap~\cite{openstreetmap} e costruisce una griglia discretizzata associata a una specifica \textit{bounding box}.

Nel servizio di correzione PIML, la \textit{building map} viene caricata da file (\texttt{.npy}) e utilizzata come canale informativo aggiuntivo insieme alle mappe di concentrazione.
Lo schema di input del servizio di correzione prevede infatti:
(i) parametri del vento;
(ii) una \texttt{concentration\_map} bidimensionale;
(iii) una \texttt{building\_map} binaria bidimensionale.

Questa scelta consente di incorporare nel flusso \textit{data-driven} un vincolo geometrico che approssima l’effetto schermante e canalizzante dell’ambiente urbano, in coerenza con l’approccio \emph{Physics-Informed} adottato.

\subsection{Feature derivate per la localizzazione della sorgente}

Per il task di localizzazione della sorgente, il Layer~2 include un insieme di \textit{feature} derivate che combinano informazioni sul vento e sulla stabilità atmosferica in variabili più informative.
Nel processo di training del modello PIML di localizzazione (\texttt{emission\_source\_piml.ipynb}) vengono utilizzate, tra le altre, \textit{feature} trigonometrizzate della direzione del vento (seno e coseno), indicatori sintetici di allineamento vento--moto e termini di interazione tra vento, stabilità e variabili ambientali.

Nel contesto architetturale, tali trasformazioni appartengono al Layer~2 poiché rappresentano un’esplicitazione di conoscenza fisica e relazionale che migliora l’apprendimento del modello di localizzazione, implementato nel Layer~3.
La corrispondenza tra \textit{feature} utilizzate in fase di training e \textit{feature} calcolate a \textit{runtime} è garantita a livello implementativo nel servizio di localizzazione dedicato.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{img/piml_feature_correlation.png}
	\caption{Matrice di correlazione tra le \textit{feature} fisiche e le \textit{feature} di interazione utilizzate dal modello PIML per la localizzazione della sorgente. Le correlazioni osservate riflettono relazioni coerenti con i meccanismi fisici di dispersione atmosferica e con la struttura delle \textit{feature} derivate.}
	\label{fig:piml_feature_correlation}
\end{figure}

\section{Layer 3 -- Inference Services}
\label{sec:layer3_inference}

Il Layer~3 rappresenta il livello di \emph{inferenza} dell’architettura di \textsc{PENTION-M} ed è responsabile dell’elaborazione dei dati e delle \textit{feature} prodotte dai layer precedenti al fine di generare output operativi interpretabili.
In questo livello sono collocati i servizi che implementano i modelli fisici, \textit{data-driven} e \emph{Physics-Informed Machine Learning}, ciascuno specializzato in uno specifico task di analisi.

Dal punto di vista architetturale, il Layer~3 è composto da un insieme di microservizi containerizzati, esposti tramite interfacce API ben definite e invocati dal Layer~1 durante l’orchestrazione della pipeline \textit{end-to-end}.
Ogni servizio opera in modo indipendente, consumando un sottoinsieme strutturato dell’evento unificato e producendo risultati che vengono reintegrati nello stesso evento come output intermedi o finali.

Nel prototipo implementato, il Layer~3 include tre principali classi di servizi:
\begin{itemize}
	\item simulazione fisica e correzione della dispersione;
	\item localizzazione della sorgente di emissione;
	\item classificazione della sostanza tramite spettrometria di massa (EI).
\end{itemize}

\paragraph{Nota sull’integrazione della classificazione NPS}

Il servizio di classificazione delle sostanze NPS, sebbene basato su una pipeline modellistica distinta, è pienamente integrato nel Layer~3 di inferenza.
L’output del classificatore (classe predetta, confidenza calibrata e candidati alternativi) viene propagato nell’evento unificato e utilizzato sia per il supporto decisionale operativo (Layer~6) sia per la produzione delle evidenze forensi (Layer~7).
I dettagli relativi al modello, alla calibrazione delle probabilità e alla valutazione delle prestazioni sono approfonditi nella sezione dedicata alla classificazione NPS.

Questa separazione riflette una precisa scelta progettuale: ogni servizio incapsula una specifica competenza modellistica, riducendo l’accoppiamento tra componenti e facilitando l’evoluzione indipendente dei modelli.
Il Layer~3 non gestisce direttamente aspetti di monitoraggio, versionamento o logging forense, che sono delegati ai layer successivi, ma si concentra esclusivamente sull’esecuzione dell’inferenza.

\subsection{Servizio di classificazione NPS (EI)}
\label{sec:layer3_nps_classification}

Il task di classificazione NPS è implementato come servizio di inferenza dedicato all’interno del Layer~3. Il servizio riceve in input uno spettro EI vettorizzato e restituisce (i) la classe predetta, (ii) le probabilità associate alle classi e (iii) metadati necessari a monitoring e logging forense.

\paragraph{Selezione del modello}
Durante la fase iniziale di sviluppo è stata condotta un’analisi comparativa tra diversi algoritmi di classificazione supervisionata, con l’obiettivo di selezionare un modello robusto e adatto al dominio spettrale EI. La Figura~\ref{fig:nps_model_accuracies} riassume le accuratezze ottenute sul dataset \texttt{PENTION\_EI\_Complete}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.85\textwidth]{img/model_accuracies.png}
	\caption{Accuratezza dei modelli valutati nella fase di selezione del classificatore NPS sul dataset \texttt{PENTION\_EI\_Complete}.}
	\label{fig:nps_model_accuracies}
\end{figure}

In base al confronto, è stato selezionato un modello basato su \textbf{XGBoost}~\cite{xgboost}, in quanto combina buone prestazioni predittive con stabilità e facilità di integrazione come servizio containerizzato.

Oltre alla scelta dell’algoritmo, è stata posta particolare attenzione all’interpretabilità del classificatore NPS.
Al fine di ridurre la dimensionalità del problema e limitare la presenza di \textit{feature} ridondanti o poco informative, è stata applicata una procedura di \emph{Recursive Feature Elimination} (RFE) basata su Random Forest.

L’analisi delle \textit{feature} selezionate consente di verificare che il modello fondi le proprie decisioni su contributi spettrali fisicamente e chimicamente significativi, aspetto rilevante in un contesto forense.

\begin{figure}[t]
	\centering
	\includegraphics[width=0.75\textwidth]{img/rfe_feature_importance.png}
	\caption{Importanza relativa delle \textit{feature} spettrali selezionate tramite Recursive Feature Elimination (RFE) per il classificatore NPS. Le \textit{feature} evidenziate risultano coerenti con contributi EI fisicamente e chimicamente interpretabili.}
	\label{fig:nps_rfe_features}
\end{figure}

\paragraph{Comportamento del classificatore sul set di test}
La Figura~\ref{fig:nps_xgb_confusion_arch} mostra la matrice di confusione del modello XGBoost sul set di test multi-classe, evidenziando anche la capacità di separare la classe negativa \emph{Other compounds} dalle classi NPS.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.70\textwidth]{img/confusion_matrix_xgb.png}
	\caption{Matrice di confusione del classificatore XGBoost (fase di selezione e integrazione del servizio).}
	\label{fig:nps_xgb_confusion_arch}
\end{figure}

\subsection{Simulazione fisica della dispersione}
\label{sec:layer3_dispersion}

Il primo servizio di inferenza del Layer~3 è dedicato alla simulazione fisica della dispersione della sostanza rilasciata nello scenario.
Questo servizio implementa un modello di dispersione di tipo \emph{Gaussian Puff}, utilizzato come \textit{baseline} fisica per descrivere la propagazione spaziale e temporale della concentrazione in atmosfera.

\paragraph{Scelta del modello: plume vs puff}

Nel contesto di \textsc{PENTION-M} è stato adottato un modello di tipo \emph{Gaussian Puff} in luogo del più tradizionale \emph{Gaussian Plume}.
Questa scelta è motivata dalla natura dinamica dello scenario simulato: il punto di campionamento è mobile e le condizioni ambientali possono variare nel tempo.
Il paradigma a \textit{puff} consente di modellare rilasci non stazionari come una sequenza di contributi discreti, risultando più adatto a contesti di pattugliamento urbano.

\paragraph{Forma del contributo gaussiano}

La concentrazione associata a un singolo \textit{puff} può essere espressa come:
\[
	C(x,y,z,t)=\frac{Q}{(2\pi)^{3/2}\sigma_x\sigma_y\sigma_z}
	\exp\!\left(
	-\frac{(x-x_p(t))^2}{2\sigma_x^2}
	-\frac{(y-y_p(t))^2}{2\sigma_y^2}
	-\frac{(z-z_p(t))^2}{2\sigma_z^2}
	\right),
\]
e la concentrazione totale come somma dei \textit{puff} attivi
$C_{\text{tot}}=\sum_i C_i(x,y,z,t)$.
Nel sistema, tale formulazione fornisce una \textit{baseline} fisicamente interpretabile per l’integrazione con i modelli PIML successivi.

Il modello riceve in ingresso le \textit{feature} fisiche prodotte dal Layer~2, tra cui i parametri del vento, la classe di stabilità atmosferica e i coefficienti di dispersione laterale e verticale $(\sigma_y, \sigma_z)$.
A partire da tali informazioni, il servizio genera una rappresentazione della distribuzione di concentrazione nello spazio, mantenendo una chiara interpretabilità fisica delle grandezze coinvolte.

Nel contesto di \textsc{PENTION-M}, la simulazione fisica non ha l’obiettivo di fornire una previsione atmosferica ad alta fedeltà, ma di costituire un riferimento modellistico coerente e controllabile, utile sia per l’analisi qualitativa dello scenario sia come base per l’integrazione con modelli PIML nei servizi successivi.

Dal punto di vista architetturale, il servizio di dispersione è esposto come microservizio indipendente e viene invocato dal Layer~1 durante la fase di orchestrazione.
Il risultato della simulazione viene reintegrato nell’evento unificato sotto forma di mappe di concentrazione strutturate, che possono essere utilizzate sia a scopo operativo sia come input per i modelli di correzione e localizzazione basati su \emph{Physics-Informed Machine Learning}.

La presenza esplicita di un modello fisico nel Layer~3 consente di mantenere un collegamento diretto tra dati osservati, assunzioni fisiche e output di inferenza, costituendo un elemento chiave per la trasparenza e la verificabilità del sistema.

\subsection{Modello PIML di correzione della dispersione}
\label{sec:layer3_piml_correction}

Il secondo servizio di inferenza del Layer~3 è dedicato alla correzione fisicamente informata dei campi di concentrazione generati dal modello di dispersione gaussiano.
Questo servizio implementa un approccio di \emph{Physics-Informed Machine Learning} (PIML) finalizzato a ridurre le discrepanze tra la simulazione fisica semplificata e una rappresentazione più realistica della dispersione in ambiente urbano.

L’obiettivo del modello non è sostituire il modello fisico di base, bensì correggerne sistematicamente gli errori strutturali, mantenendo coerenza con i vincoli fisici noti e con il contesto geometrico dello scenario.
In questo senso, il modello PIML opera come un \emph{correction layer} che apprende deviazioni sistematiche dovute a fenomeni non esplicitamente modellati, quali l’interazione con l’ambiente urbano e la complessità dei flussi locali.

\paragraph{Architettura del modello MCxM--PIML}

Il modello adottato è una rete neurale convoluzionale bidimensionale, implementata nel modulo \texttt{MCxM\_PIML.py}, progettata per operare su mappe spaziali di concentrazione.
L’architettura segue uno schema encoder--decoder di tipo \emph{U-Net-like}~\cite{ronneberger_unet}, in cui le operazioni convoluzionali consentono di catturare \textit{pattern} spaziali sia locali sia globali rilevanti per il fenomeno di dispersione.

L’input del modello è multi-canale e comprende:
(i) la mappa di concentrazione prodotta dal modello gaussiano;
(ii) una \emph{building map} binaria che rappresenta la presenza di edifici nel contesto urbano;
(iii) un insieme di parametri fisici scalari associati allo scenario, tra cui velocità e direzione del vento, classe di stabilità atmosferica e umidità relativa.
Questa rappresentazione consente al modello di apprendere correzioni spazialmente dipendenti, modulandole in funzione sia del campo di concentrazione sia del contesto geometrico e ambientale.

L’output del modello è una mappa di concentrazione corretta, della stessa dimensionalità dell’input, progettata per approssimare un campo di dispersione più realistico rispetto alla simulazione fisica di base.

\paragraph{Integrazione dei vincoli fisici nella funzione di loss}

Un elemento centrale dell’approccio PIML adottato è la definizione di una funzione di \textit{loss} fisicamente informata, implementata nel modulo \texttt{loss\_function\_piml.py}.
La funzione di perdita combina una componente di errore predittivo con termini di penalizzazione che riflettono vincoli fisici espliciti.

In particolare, la \textit{loss} include contributi associati a:
(i) accuratezza della previsione rispetto al campo di riferimento;
(ii) regolarità spaziale del campo corretto, al fine di evitare discontinuità non fisicamente plausibili;
(iii) penalizzazione della presenza di concentrazione all’interno delle aree edificate;
(iv) coerenza tra il gradiente spaziale del campo di concentrazione e la direzione del vento dominante.
Questo approccio guida l’addestramento verso soluzioni che risultano non solo numericamente accurate, ma anche fisicamente coerenti, riducendo il rischio di \textit{overfitting} puramente \textit{data-driven}.

\paragraph{Dataset PIML e pipeline di addestramento}

Il modello è addestrato su un dataset simulato specificamente progettato per il contesto PIML.
I dati vengono generati tramite lo script \texttt{simulation\_gen.py} e successivamente preprocessati mediante \texttt{dataset\_preprocessing\_piml.py}.
Per ciascuna simulazione, il dataset include:
(i) parametri ambientali e meteorologici;
(ii) mappe di concentrazione generate dal modello fisico;
(iii) mappe di riferimento della dispersione ``reale'';
(iv) maschere urbane coerenti con la \textit{bounding box} geografica dello scenario.

La pipeline di training, implementata in \texttt{cnn\_train\_pipeline\_piml.py}, gestisce la suddivisione in training e validation set, il caricamento dei dati tramite la classe \texttt{CNNDataset} e l’ottimizzazione dei pesi del modello.
L’andamento della \textit{loss} su training e validation è utilizzato per monitorare la stabilità dell’addestramento e verificare l’assenza di fenomeni di \textit{overfitting}.

\paragraph{Esposizione come servizio di inferenza}

Una volta addestrato, il modello PIML viene serializzato e caricato dal servizio di inferenza \texttt{service\_correction\_piml.py}, esposto tramite API REST definite in \texttt{api\_correction\_piml.py}.
Il servizio riceve in ingresso le mappe di concentrazione e i parametri di scenario prodotti dai layer precedenti e restituisce una mappa corretta che viene integrata nell’evento unificato della pipeline.

Dal punto di vista architetturale, il servizio è \textit{stateless} e completamente containerizzato, garantendo riproducibilità delle inferenze e integrazione trasparente nel flusso \textit{end-to-end}.
Le informazioni relative alla versione del modello e ai parametri di esecuzione vengono propagate ai layer di Monitoring e ModelOps per finalità di tracciabilità e auditabilità.

\paragraph{Ruolo del modello PIML nel Layer di inferenza}

Il modello PIML di correzione della dispersione rappresenta un punto di equilibrio tra modellazione fisica e apprendimento automatico.
All’interno del Layer~3, esso agisce come ponte tra simulazione fisica e inferenza avanzata, migliorando l’affidabilità delle mappe di concentrazione utilizzate dai successivi moduli di localizzazione della sorgente e di valutazione operativa del rischio.

\subsection{Modello di localizzazione della sorgente}
\label{sec:layer3_source_localization}

Il problema di localizzazione della sorgente di emissione può essere formulato come un \emph{inverse advection-diffusion problem}; in tale classe di problemi, approcci basati su \emph{Physics-Informed Neural Networks} sono stati recentemente impiegati per stimare parametri o condizioni iniziali a partire da osservazioni parziali~\cite{chuprov2025inverse}.

Il terzo servizio di inferenza del Layer~3 è dedicato alla \emph{localizzazione della sorgente di emissione} della sostanza rilevata, ovvero alla stima delle coordinate spaziali della sorgente responsabile del campo di concentrazione osservato dai sensori.
Questo task rappresenta un passaggio cruciale nella pipeline operativa di \textsc{PENTION-M}, in quanto traduce l’informazione di dispersione in un output direttamente utilizzabile a fini investigativi e decisionali.

Nel prototipo implementato, la localizzazione della sorgente è realizzata mediante un modello di \emph{Physics-Informed Machine Learning} (PIML), progettato per integrare esplicitamente informazione fisica e geometrica nel processo di inferenza, riducendo l’ambiguità tipica degli approcci puramente \textit{data-driven}.

\paragraph{Feature fisiche utilizzate}

Il modello di localizzazione opera su un insieme di \textit{feature} derivate prodotte dal Layer~2, che rendono esplicite le principali grandezze fisiche coinvolte nel fenomeno di dispersione.
In particolare, l’input del modello include:

\begin{itemize}
	\item componenti cartesiane della direzione del vento ($\cos(\theta)$, $\sin(\theta)$), che evitano discontinuità angolari;
	\item intensità del vento (\texttt{wind\_speed});
	\item indicatori di stabilità atmosferica, derivati dalla classificazione di Pasquill--Gifford;
	\item coordinate spaziali dei sensori operativi (\texttt{gps\_x}, \texttt{gps\_y});
	\item serie temporali delle concentrazioni misurate dai sensori;
\end{itemize}

Questa rappresentazione consente al modello di apprendere relazioni fisicamente plausibili tra la distribuzione spaziale delle misure, le condizioni ambientali e la posizione della sorgente.
L’utilizzo esplicito di \textit{feature} fisiche riduce la dimensionalità latente del problema e migliora la stabilità dell’inferenza in scenari eterogenei.

\paragraph{Architettura e addestramento del modello PIML}

Il modello di localizzazione è addestrato offline utilizzando il notebook \texttt{emission\_source\_piml.ipynb}, a partire da dataset simulati coerenti con gli scenari operativi di \textsc{PENTION-M}.
L’addestramento sfrutta una normalizzazione consistente delle \textit{feature}, serializzata nel file \texttt{scaler\_piml.pkl}, al fine di garantire coerenza tra fase di training e fase di inferenza \textit{runtime}.

Il modello finale, salvato come \texttt{emission\_source\_model\_piml.pkl}, implementa una regressione multivariata che stima direttamente le coordinate spaziali della sorgente.
L’approccio PIML consente di limitare soluzioni non fisicamente plausibili, vincolando implicitamente lo spazio delle predizioni alle configurazioni compatibili con la dinamica della dispersione atmosferica.

\paragraph{Stabilità dell’inferenza e output del servizio}

Dal punto di vista operativo, il modello di localizzazione è esposto come microservizio \textit{stateless} tramite API REST definite in \texttt{api\_source\_localization\_piml.py} e implementate nel servizio \texttt{service\_source\_localization\_piml.py}.
Il servizio riceve in ingresso i dati strutturati dei sensori e restituisce una stima puntuale della posizione della sorgente, accompagnata da metadati di esecuzione e versioning del modello.

Le metriche di validazione ottenute in fase di test mostrano una buona stabilità del modello, con errori medi dell’ordine di alcune decine di metri, compatibili con la risoluzione spaziale dello scenario simulato.
In particolare, i valori di RMSE e MAE confermano la capacità del modello di fornire stime robuste anche in presenza di rumore e di parziale incompletezza delle misure.

All’interno dell’architettura di \textsc{PENTION-M}, l’output del servizio di localizzazione viene reintegrato nell’evento unificato della pipeline e reso disponibile sia per la visualizzazione operativa (Layer~6) sia per la produzione di evidenze forensi verificabili (Layer~7).
In questo modo, il modello di localizzazione della sorgente completa il Layer~3 di inferenza, fornendo un collegamento diretto tra modellazione fisica, apprendimento automatico e supporto decisionale.

\section{Layer 4 -- Monitoring e Telemetry}
\label{sec:layer4_monitoring}

Il Layer~4 rappresenta il livello di \emph{monitoring operativo e telemetry} di \textsc{PENTION-M} ed è responsabile dell’osservazione continua del comportamento della pipeline durante l’esecuzione.
A differenza di approcci MLOps tradizionali orientati esclusivamente a metriche statistiche di accuratezza offline, questo layer è progettato per monitorare \emph{proprietà runtime fisicamente e operativamente rilevanti}, in coerenza con la natura \textit{cyber-fisica} e forense del sistema.

Il servizio di monitoring è implementato come microservizio containerizzato (\texttt{monitoring\_service.py}) ed è invocato automaticamente dal Layer~1 al termine di ogni ciclo di inferenza \textit{end-to-end}.
Il layer non interviene direttamente nel processo decisionale, ma fornisce una visione strutturata dello stato del sistema, abilitando controlli di stabilità, affidabilità e auditabilità.

\subsection{Ruolo del monitoring nel contesto PENTION-M}

Nel contesto di \textsc{PENTION-M}, il monitoring assolve a tre funzioni principali:
\begin{itemize}
	\item \textbf{controllo operativo runtime}, monitorando latenza, \textit{drift} e stabilità delle predizioni durante l’esecuzione;
	\item \textbf{supporto alla pipeline ModelOps}, fornendo segnali quantitativi per la gestione delle versioni e dei retraining;
	\item \textbf{tracciabilità forense}, producendo log strutturati e immutabili utilizzabili per audit e validazione ex-post.
\end{itemize}

Questa impostazione riflette una precisa scelta progettuale: il monitoring non è trattato come componente accessoria, ma come parte integrante dell’architettura di validazione \emph{by construction} dell’artefatto.

\subsection{Struttura dell’evento di monitoring}

Il Layer~4 riceve in ingresso l’evento unificato prodotto dal Layer~1, che include informazioni sui sensori, sulle \textit{feature} PIML, sui risultati di inferenza e sui metadati ModelOps.
A partire da tale evento, il servizio di monitoring estrae un sottoinsieme coerente di informazioni rilevanti, organizzate in un blocco di \textit{telemetry} strutturato.

In particolare, per ogni simulazione vengono monitorate:
\begin{itemize}
	\item la latenza complessiva della pipeline (\texttt{latency\_ms});
	\item la versione del modello attivo (\texttt{model\_version});
	\item indicatori di stabilità fisica e modellistica;
	\item la confidenza associata alle predizioni;
	\item un indice sintetico di \textit{drift} (\texttt{drift\_score}).
\end{itemize}

Tali informazioni vengono serializzate in formato \textit{JSON Lines}\footnote{\url{https://jsonlines.org/}} all’interno del file \texttt{monitoring\_log.jsonl}, costituendo una traccia temporale ordinata degli eventi di esecuzione del sistema.

\subsection{Monitoraggio della latenza}

La latenza rappresenta una metrica critica nel contesto operativo di un laboratorio mobile.
Il Layer~4 distingue tra:
\begin{itemize}
	\item latenza \textit{end-to-end} osservata dal client;
	\item latenza interna misurata dal sistema durante la pipeline.
\end{itemize}

La latenza interna è calcolata utilizzando \textit{timestamp} coerenti propagati lungo il flusso di \textit{ingestion} e inferenza.
Questa misura consente di valutare il carico computazionale dei singoli componenti e di identificare condizioni di stress o degrado prestazionale.

Nel contesto simulato di \textsc{PENTION-M}, latenze dell’ordine di alcuni secondi sono considerate accettabili, in quanto il sistema esegue:
(i) simulazioni fisiche costose;
(ii) modelli PIML convoluzionali su mappe bidimensionali;
(iii) pipeline di classificazione e logging forense.
Il monitoring consente di caratterizzare tali latenze e di contestualizzarle correttamente.

\subsection{Drift fisicamente informato}

Un elemento distintivo del Layer~4 è l’introduzione di un \emph{drift fisicamente informato}. A differenza del classico \emph{concept drift} statistico descritto in letteratura~\cite{gama_drift}, questo indicatore è progettato per catturare variazioni anomale nel comportamento del sistema rispetto a una \textit{baseline} operativa.

Il \textit{drift} non è calcolato esclusivamente sulle \textit{feature} di input, ma su un vettore fisico composito che include:
\begin{itemize}
	\item parametri di dispersione ($\sigma_y$, $\sigma_z$);
	\item indicatori di stabilità atmosferica;
	\item numero di Péclet e grandezze di trasporto;
	\item velocità e direzione del vento;
	\item confidenza dei modelli di inferenza.
\end{itemize}

A partire da questo vettore, il servizio implementa un meccanismo di \textit{drift} basato sulla distanza di Mahalanobis rispetto a una \textit{baseline} incrementale, memorizzata nel file \texttt{drift\_baseline.json}.
Durante le prime esecuzioni, la \textit{baseline} viene costruita progressivamente; una volta stabilizzata, il \textit{drift} viene normalizzato rispetto alla distribuzione storica.

Il risultato è un valore scalare $\in [0,1]$ che quantifica la deviazione dello stato corrente del sistema rispetto al comportamento atteso, senza richiedere accesso alla \textit{ground truth} reale.
Questo approccio è particolarmente adatto a contesti fisico--forensi, in cui il concetto di \textit{drift} deve riflettere anche coerenza fisica e affidabilità operativa.

\subsection{Stabilità e confidenza delle predizioni}

Oltre al \textit{drift}, il Layer~4 monitora indicatori di stabilità delle predizioni prodotte dai modelli PIML e dal classificatore NPS.
In particolare, vengono tracciate:
\begin{itemize}
	\item la confidenza associata alle predizioni di classificazione;
	\item la coerenza delle stime di localizzazione della sorgente;
	\item variazioni improvvise di output a parità di condizioni fisiche.
\end{itemize}

Queste informazioni consentono di individuare situazioni di \textit{overconfidence}, instabilità numerica o degrado progressivo delle prestazioni, fornendo segnali utili sia all’operatore umano sia ai meccanismi ModelOps dei layer successivi.

\subsection{Persistenza e auditabilità dei log}

Tutti i dati di monitoring vengono persistiti in forma \textit{append-only}, senza modifiche retroattive, garantendo l’integrità temporale delle informazioni.
Il formato \textit{JSON Lines} consente analisi batch, visualizzazioni offline e integrazione diretta con le procedure di validazione descritte nel Capitolo~5.

Dal punto di vista architetturale, il Layer~4 costituisce il ponte tra inferenza e ModelOps: le metriche prodotte non influenzano direttamente l’output operativo immediato, ma alimentano i processi di controllo, versioning e logging forense implementati nel Layer~5.

In questo modo, il monitoring non si limita a osservare il sistema, ma contribuisce attivamente alla sua validazione, robustezza e affidabilità complessiva.

\section{Layer 5 -- ModelOps e logging forense}
\label{sec:layer5_modelops_forensic}

Il Layer~5 rappresenta il livello di \emph{ModelOps e logging forense} di \textsc{PENTION-M} ed è responsabile della gestione del ciclo di vita dei modelli, della tracciabilità delle configurazioni di esecuzione e della produzione di evidenze digitali verificabili.
Questo layer costituisce un elemento chiave dell’artefatto, in quanto traduce le esigenze di auditabilità, riproducibilità e affidabilità emerse in fase di \emph{Requirements Engineering} in meccanismi tecnici espliciti e verificabili.

A differenza di pipeline MLOps orientate esclusivamente all’automazione del retraining o all’ottimizzazione continua delle prestazioni, \textsc{PENTION-M} adotta un approccio \emph{conservativo e forense-aware}: le decisioni operative sono sempre accompagnate da metadati completi, e ogni esecuzione produce un \textit{audit trail} immutabile.
In questo senso, il Layer~5 non è progettato per intervenire dinamicamente sull’inferenza \textit{runtime}, ma per garantire che ogni risultato prodotto dal sistema sia contestualizzato, verificabile e difendibile ex-post.

\subsection{Ruolo del ModelOps nel contesto PENTION-M}

Nel contesto di \textsc{PENTION-M}, il termine \emph{ModelOps} è inteso in senso esteso e include non solo il versionamento dei modelli, ma l’intera gestione del loro utilizzo operativo.
Il Layer~5 assolve in particolare a quattro funzioni principali:
\begin{itemize}
	\item tracciamento delle versioni dei modelli e delle configurazioni attive;
	\item associazione esplicita tra modelli, dati di input e output prodotti;
	\item registrazione degli eventi rilevanti per il ciclo di vita dei modelli;
	\item produzione di evidenze forensi verificabili e immutabili.
\end{itemize}

Questo approccio riflette una scelta progettuale deliberata: in un contesto investigativo e forense, la priorità non è l’adattamento automatico dei modelli, bensì la possibilità di ricostruire in modo completo e indipendente il processo decisionale seguito dal sistema in un determinato istante temporale.

Questa interpretazione estesa del ModelOps è coerente con gli approcci proposti per sistemi cyber-fisici e \textit{safety-critical}, in cui la gestione del ciclo di vita dei modelli deve integrare monitoring, tracciabilità e requisiti di affidabilità operativa~\cite{faubel2025mlops_cps}.

\subsection{Versioning dei modelli e audit trail}

Il versionamento dei modelli è gestito tramite un registro persistente (\texttt{model\_registry.json}) che associa a ciascun modello utilizzato le informazioni rilevanti per la sua identificazione e riproducibilità.
Per ogni servizio di inferenza vengono tracciati:
\begin{itemize}
	\item identificativo e versione del modello;
	\item tipologia di modello (fisico, PIML, \textit{data-driven});
	\item data di registrazione e di attivazione;
	\item riferimento alla configurazione di training;
\end{itemize}

Durante l’esecuzione della pipeline, il Layer~1 propaga nell’evento unificato l’identificativo delle versioni di modello effettivamente utilizzate.
Il Layer~5 raccoglie tali informazioni e le persiste come parte dell’\textit{audit trail}, garantendo che ogni output possa essere ricondotto in modo univoco agli artefatti software che lo hanno generato.

Le operazioni di retraining non avvengono in modo opaco o non supervisionato, ma sono attivate automaticamente in risposta a condizioni operative esplicitamente monitorate dal sistema.
In particolare, eventi di \textit{drift}, instabilità o degrado delle prestazioni possono generare automaticamente un \textit{trigger} di retraining, gestito e registrato dal sottosistema ModelOps.

Ogni evento di retraining viene tracciato separatamente nel file \texttt{modelops\_retrain\_log.jsonl}, che documenta in modo esplicito:
(i) la condizione che ha innescato il retraining;
(ii) la versione precedente e quella candidata del modello;
(iii) l’esito dell’operazione.

Questa separazione tra rilevazione automatica, esecuzione controllata e tracciamento persistente consente di mantenere un elevato livello di automazione, senza compromettere i requisiti di auditabilità e controllo umano richiesti in un contesto forense.

\subsection{Logging forense e struttura del bundle}

Il cuore del Layer~5 è rappresentato dal sistema di logging forense, implementato nel modulo \texttt{forensic\_logger.py}.
Al termine di ogni esecuzione completa della pipeline, l’evento unificato viene trasformato in un \emph{forensic bundle}, ovvero un pacchetto digitale autosufficiente che rappresenta l’evidenza dell’attività svolta dal sistema.

Il processo di generazione del \textit{bundle} prevede:
\begin{enumerate}
	\item serializzazione canonica dell’evento unificato;
	\item calcolo dell’hash crittografico tramite algoritmo SHA--256~\cite{rfc6234_sha};
	\item firma digitale del \textit{digest} mediante algoritmo Ed25519~\cite{rfc8032_eddsa};
	\item inclusione della chiave pubblica necessaria alla verifica.
\end{enumerate}

Il \textit{bundle} risultante contiene:
\begin{itemize}
	\item l’evento completo, comprensivo di input, output e metadati;
	\item l’hash SHA--256 dell’evento serializzato;
	\item la firma digitale;
	\item la chiave pubblica associata;
	\item informazioni di supporto alla verifica e alla compliance.
\end{itemize}

Ogni \textit{bundle} è persistito in forma \textit{append-only} all’interno della directory \texttt{logs/forensic/} e identificato da un nome univoco che include \textit{timestamp} e identificativo di simulazione.
Una volta generato, il \textit{bundle} non viene mai modificato, garantendo l’immutabilità dell’evidenza.

\subsection{Firma digitale e verifica dell’integrità}

La firma digitale Ed25519 consente di verificare in modo indipendente sia l’integrità sia l’autenticità del \textit{bundle}.
Il processo di verifica, implementato nello script \texttt{verify\_bundle.py}, ricostruisce l’evento in forma canonica, ricalcola l’hash SHA--256 e verifica la firma mediante la chiave pubblica inclusa nel \textit{bundle} stesso.

\paragraph{Verifica zero-trust e inclusione dei compliance tag}

Il processo di verifica adotta un approccio \emph{zero trust}: l’hash non viene considerato affidabile in quanto dichiarato nel \textit{bundle}, ma viene ricalcolato ex-novo a partire dall’evento serializzato in forma canonica.
La firma digitale è quindi verificata utilizzando l’hash ricalcolato e la chiave pubblica inclusa, rendendo immediatamente rilevabile qualsiasi alterazione del contenuto.

I tag di compliance e di audit (\textit{compliance tag}) vengono inseriti nell’evento prima della fase di hashing e firma.
In questo modo, tali informazioni diventano parte integrante del contenuto firmato e non possono essere modificate a posteriori senza invalidare l’intera evidenza forense.

Questo meccanismo consente di rilevare qualsiasi alterazione, anche minima, dei dati registrati.
Test di manomissione controllata, implementati nello script \texttt{tamper\_test.py}, mostrano che la modifica di un singolo campo dell’evento (ad esempio una coordinata GPS) invalida immediatamente sia il confronto degli hash sia la verifica della firma digitale.
Tali test confermano la robustezza del meccanismo di protezione e la sua idoneità a contesti forensi.

\subsection{Chain of custody digitale}

L’insieme di versioning, logging strutturato, hashing e firma digitale implementato nel Layer~5 realizza una forma di \emph{chain of custody} digitale.
Ogni evidenza prodotta dal sistema è:
\begin{itemize}
	\item temporalmente collocabile;
	\item riconducibile a una specifica configurazione di modelli;
	\item verificabile in modo indipendente;
	\item resistente a manomissioni non autorizzate.
\end{itemize}

Questi requisiti sono in linea con le raccomandazioni discusse in ambito \emph{SecMLOps} per garantire auditabilità e integrità degli artefatti in pipeline di ML in produzione~\cite{calefato_secmlops}.

Dal punto di vista architetturale, il Layer~5 separa in modo esplicito le metriche di monitoring operativo (Layer~4) dalle evidenze forensi, evitando commistioni tra osservazione del comportamento del sistema e produzione di prove digitali.
Questa separazione rafforza la chiarezza concettuale del sistema e facilita la validazione ex-post delle decisioni prese.

\subsection{Collegamento con la validazione by construction}

Il Layer~5 costituisce un elemento fondamentale della validazione \emph{by construction} di \textsc{PENTION-M}.
La produzione sistematica di \textit{bundle} forensi firmati garantisce che ogni iterazione dell’artefatto e ogni esecuzione della pipeline siano documentate in modo completo e verificabile.

In questo senso, il logging forense non rappresenta un’aggiunta a posteriori, ma una componente strutturale dell’architettura.
Le proprietà di auditabilità, riproducibilità e tracciabilità non sono valutate tramite un singolo esperimento finale, bensì emergono direttamente dal modo in cui il sistema è stato progettato e implementato.
Il Capitolo~5 approfondirà questo aspetto, mostrando come tali meccanismi abbiano supportato la valutazione tecnica e il confronto con gli stakeholder nel corso delle iterazioni del progetto.

\section{Layer 6 -- Interfaccia utente operativa e demo live}
\label{sec:layer6_ui}

Il Layer~6 fornisce il punto di contatto operativo tra l’analista e la pipeline \textit{end-to-end} di \textsc{PENTION-M}.
L’obiettivo dell’interfaccia non è soltanto ``mostrare un output'', ma \emph{rendere ispezionabile} l’intero ciclo di rilevazione: movimento del laboratorio mobile, attivazione della pipeline MLOps, visualizzazione delle metriche di monitoring e accesso immediato alle evidenze forensi prodotte dal sistema.

Dal punto di vista implementativo, la UI è esposta come servizio web leggero (\texttt{PentionSystem\_M/ui\_pention\_m.py}) che:
(i) serve asset statici (\texttt{index.html}, \texttt{script.js}, \texttt{style.css});
(ii) mantiene uno stato di simulazione lato server (es. \textit{Idle}/\textit{Running});
(iii) comunica aggiornamenti in tempo reale al browser tramite un canale WebSocket (\texttt{/ws}).
Questa scelta progettuale consente di ottenere una visualizzazione reattiva senza introdurre dipendenze pesanti, mantenendo coerenza con lo scenario \emph{edge-only} e \emph{offline-capable} del laboratorio mobile.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth]{img/UI0.png}
	\caption{Interfaccia operativa di \textsc{PENTION-M} durante la fase di \emph{patrolling}. Il laboratorio mobile simula un’attività di perlustrazione urbana, muovendosi lungo la rete stradale dello scenario e monitorando passivamente le condizioni ambientali.}
	\label{fig:ui_patrolling}
\end{figure}

\subsection{Componenti visuali e logica di interazione}

L’interfaccia è organizzata in due aree principali:

\begin{itemize}
	\item \textbf{Mappa operativa (sinistra)}: una mappa interattiva basata su Leaflet\footnote{\url{https://leafletjs.com/}}, alimentata da OpenStreetMap, su cui viene renderizzato il van e la sua evoluzione spaziale nello scenario urbano. La navigazione del veicolo è vincolata a una rete stradale pre-caricata da cache (\texttt{osm\_cache/amsterdam\_drive.graphml}), ottenendo un comportamento coerente con un pattugliamento urbano realistico.
	\item \textbf{Pannello di controllo e telemetry (destra)}: un insieme di \textit{card} che sintetizzano lo stato della simulazione e i risultati della pipeline (\emph{Simulation info}, \emph{EI Mass Spectrum}, \emph{Last forensic bundle}).
\end{itemize}

La UI espone tre comandi operativi che riflettono il \textit{workflow} d’uso:

\begin{itemize}
	\item \textbf{Start simulation}: avvia una simulazione standard in cui il van si muove secondo logica di \textit{patrolling}; la pipeline MLOps viene attivata quando viene soddisfatta la condizione di \textit{detection} (cioè quando lo scenario simulato produce un evento coerente con la presenza della sorgente).
	\item \textbf{Reset}: reinizializza lo stato della simulazione e della visualizzazione.
	\item \textbf{Debug: Start near source}: modalità di debug usata per validazioni rapide, in cui il van viene inizializzato in prossimità della sorgente; la pipeline \textit{downstream} è identica alla modalità standard, ma con tempi di attivazione più rapidi.
\end{itemize}

\paragraph{Logica di detection e stabilizzazione}

L’attivazione della pipeline \textit{downstream} non è continua, ma vincolata a una condizione di \textit{detection} basata sulla prossimità spaziale del veicolo rispetto all’area di interesse.
La \textit{detection} viene valutata tramite distanza geodetica e costituisce il \textit{trigger} operativo per l’invocazione della pipeline MLOps.

Per evitare oscillazioni spurie dovute a micro-variazioni della posizione (GPS jitter), è introdotto un meccanismo di isteresi spaziale che stabilizza la condizione di ingresso e uscita dall’area di \textit{detection}.
Questa soluzione migliora la riproducibilità della demo e rende il comportamento del sistema più coerente con uno scenario operativo reale.

\subsection{Aggiornamento real-time e integrazione con la pipeline}

Durante l’esecuzione, il backend invia al client aggiornamenti incrementali tramite WebSocket.
Il browser riceve tali messaggi e aggiorna in tempo reale:

\begin{itemize}
	\item \textbf{stato della simulazione} (es. \textit{Idle}/\textit{Running}) e identificativo (\texttt{simulation\_id});
	\item \textbf{model versioning} riportato dalla pipeline (es. \texttt{PIML\_v8});
	\item \textbf{metriche di monitoring} prodotte dal Layer~4: \textit{drift score}, latenza, stability index e confidenza;
	\item \textbf{spettro EI corrente} (card \emph{EI Mass Spectrum}), renderizzato come grafico a barre/linea per supportare un controllo visivo immediato del segnale;
	\item \textbf{riassunto dell’ultimo bundle forense} generato, con campi essenziali per triage operativo (predizione del modello, confidenza, vento, coordinate GPS e hash).
\end{itemize}

Questa modalità di aggiornamento è progettata per essere \emph{operativamente leggibile}: le metriche non sono presentate come dettagli tecnici isolati, ma come indicatori di stato che consentono all’operatore di valutare rapidamente (i) l’affidabilità della predizione, (ii) l’eventuale presenza di condizioni anomale (\textit{drift} elevato), e (iii) il costo temporale della pipeline (latenza \textit{end-to-end}).

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth]{img/UI1.png}
	\caption{Stato operativo del sistema a seguito della \textit{detection}. Il laboratorio mobile interrompe il \textit{patrolling} ed entra in stato \emph{Idle}, mentre la barra laterale mostra i risultati sintetici prodotti dalla pipeline \textit{end-to-end}, incluse metriche di monitoring, output di inferenza e riferimenti al \textit{bundle} forense.}
	\label{fig:ui_detected_idle}
\end{figure}

\subsection{Report operativo e collegamento alle evidenze forensi}

Un requisito centrale emerso in fase progettuale è la possibilità di passare dalla visualizzazione operativa alla verifica forense senza salti manuali.
Per questo motivo la UI include una vista dedicata al \emph{Forensic Detection Report}, accessibile dalla card \emph{Last forensic bundle} tramite il comando \emph{View full report}.

Il report è costruito a partire dai metadati già presenti nel \textit{bundle} generato dal Layer~7, e organizza in modo leggibile:
(i) identificativi di simulazione e timestamp;
(ii) output di classificazione (classe e confidenza);
(iii) condizioni meteo e parametri di contesto rilevanti;
(iv) riferimenti di integrità (hash e identificativo del bundle).
In aggiunta, la UI include una funzione di esportazione in PDF (azione \emph{Download PDF}) per produrre rapidamente un artefatto condivisibile e stampabile, senza compromettere la catena di tracciabilità: il PDF rappresenta una vista umana del contenuto, mentre la verifica crittografica resta ancorata al \textit{bundle} firmato generato dal sistema.

\begin{figure}[H]
	\centering
	\begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{img/UI2.png}
		\caption{Sezione iniziale del report forense visualizzato nella UI, con spettro EI, feature PIML e parametri fisici di scenario.}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{img/UI3.png}
		\caption{Sezione di inferenza e monitoring del report, comprendente predizione del modello, confidenza, latenza e stato delle componenti.}
	\end{subfigure}
	\caption{Vista completa del \emph{Forensic Detection Report} accessibile dall’interfaccia operativa di \textsc{PENTION-M}.}
	\label{fig:ui_full_report}
\end{figure}

\subsection[Ruolo della UI nella validazione by-construction]{Discussione: ruolo della UI nella validazione by-construction}

Nel contesto di \textsc{PENTION-M}, l’interfaccia non è un componente accessorio, ma parte integrante del paradigma di validazione \emph{by construction}.
La UI rende espliciti (e quindi discutibili e verificabili) gli elementi chiave del ciclo: la versione di modello effettivamente utilizzata, le metriche \textit{runtime} prodotte dal monitoring, e l’esistenza di un’evidenza forense immediatamente ispezionabile.
In questo modo, l’operatore (o un auditor) può osservare l’intero flusso Patrolling $\rightarrow$ Detection $\rightarrow$ Inference $\rightarrow$ Monitoring $\rightarrow$ Forensic bundle, riducendo il rischio di \emph{black-boxing} operativo e migliorando la riproducibilità complessiva degli esperimenti.

\begin{figure}[H]
	\centering
	\begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{img/PDF1.png}
		\caption{Prima sezione del report forense esportato in formato PDF, con mappa operativa e contesto dell’evento.}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{img/PDF2.png}
		\caption{Sezione del report PDF dedicata ai metadati ModelOps, agli hash degli artefatti e ai tag di compliance.}
	\end{subfigure}
	\caption{Esempio di report forense esportato da \textsc{PENTION-M} in formato PDF, derivato dal \textit{bundle} JSON firmato e progettato per consultazione umana e archiviazione documentale.}
	\label{fig:pdf_report}
\end{figure}

\section{Layer 7 -- Forensic Export e reporting}
\label{sec:layer7_forensic_export}

Il Layer~7 rappresenta il livello di \emph{Forensic Export} di \textsc{PENTION-M} e ha l’obiettivo di trasformare l’esecuzione \textit{end-to-end} della pipeline in un insieme di \textbf{evidenze digitali esportabili}, pensate per due scopi complementari:
(i) \emph{preservazione} e \emph{verificabilità} (output \textit{machine-readable}, orientato ad audit);
(ii) \emph{comunicazione operativa} (output \textit{human-readable}, orientato a consultazione e reporting).

Coerentemente con i requisiti forensi emersi durante la progettazione dell’artefatto, l’export non è trattato come una semplice funzione di ``salvataggio risultati'', ma come una fase strutturata di \emph{chain of custody} digitale: l’output deve poter essere archiviato, trasferito e verificato ex-post da terze parti, senza dipendere dall’ambiente di esecuzione originale.

\subsection{Export in formato JSON: bundle forense verificabile}

Il formato primario di esportazione è un \textbf{bundle JSON} che incapsula in modo auto-consistente le informazioni necessarie a ricostruire l’esecuzione della pipeline.
In particolare, il \textit{bundle} include:
(i) l’\emph{evento unificato} completo (sensori, feature PIML, output di inferenza, metadati);
(ii) un \textit{digest} crittografico calcolato sull’evento in forma canonica (SHA--256);
(iii) una firma digitale del \textit{digest} (Ed25519) e la chiave pubblica per la verifica.

La presenza congiunta di hash e firma consente di verificare due proprietà fondamentali:
\textbf{integrità} (nessuna modifica al contenuto) e \textbf{autenticità} (provenienza dal sistema che detiene la chiave privata).
A livello implementativo, la verifica può essere eseguita in modo indipendente tramite uno script dedicato che:
(i) ricalcola l’hash a partire dall’evento serializzato in forma canonica;
(ii) confronta l’hash calcolato con quello contenuto nel \textit{bundle};
(iii) verifica la firma Ed25519 usando la chiave pubblica inclusa nel \textit{bundle}.
Questo meccanismo rende l’evidenza \emph{self-verifying} e adatta a contesti in cui l’audit avviene al di fuori del perimetro del sistema.

\subsection{Export in formato PDF: report human-readable}

Accanto all’export JSON (orientato alla verifica), \textsc{PENTION-M} fornisce un output di tipo \textbf{report} pensato per l’operatore e per attività di consultazione rapida.
Nel prototipo, la generazione del report è integrata nell’interfaccia utente: a partire dall’ultimo \textit{bundle} disponibile, la UI costruisce una vista strutturata che riassume i principali elementi della simulazione, tra cui:
(i) identificativi e timestamp;
(ii) configurazione e versioning dei modelli utilizzati;
(iii) predizione di classificazione e relativa confidenza;
(iv) stima di localizzazione della sorgente;
(v) condizioni meteo e contesto (vento, GPS);
(vi) riferimenti di integrità (hash) utili a collegare il report al \textit{bundle} JSON originale.

Il PDF non sostituisce il \textit{bundle} forense (che resta la \emph{fonte primaria} per verifica e riproducibilità), ma funge da \emph{derivato human-readable}: un ``verbale tecnico'' che facilita la comunicazione e l’archiviazione documentale, mantenendo un collegamento esplicito con l’evidenza crittograficamente verificabile.

\subsection{Riproducibilità e audit esterno}

Il Layer~7 è progettato per supportare \textbf{riproducibilità} e \textbf{audit esterno} senza dipendenze implicite dall’ambiente di esecuzione.
In particolare, la riproducibilità è abilitata da tre scelte architetturali:

\begin{itemize}
	\item \textbf{Serializzazione canonica dell’evento}: l’hash viene calcolato su una rappresentazione deterministica dell’evento, evitando ambiguità dovute a ordinamenti o formati.

	\item \textbf{Bundle auto-consistente}: l’evidenza contiene sia il contenuto (evento) sia i materiali minimi per verificarlo (hash, firma, chiave pubblica), riducendo la necessità di dipendenze esterne per controlli di integrità.

	\item \textbf{Separazione tra export e validazione}: le procedure di validazione e test (sviluppate a fine progetto per controlli e regressione) non sono parte del \textit{runtime} operativo della pipeline, ma possono essere eseguite offline a supporto di audit e verifiche ex-post.
\end{itemize}

\paragraph{Autenticità dei dati e natura simulata}

Sebbene \textsc{PENTION-M} operi in un contesto simulato, è importante distinguere tra diverse tipologie di dati prodotte dal sistema.
I meccanismi crittografici, le metriche di monitoring e gli output dei modelli di inferenza sono generati dinamicamente durante l’esecuzione reale della pipeline containerizzata.

Le variabili ambientali e di scenario, pur essendo simulate, sono prodotte secondo dinamiche controllate e tracciate, senza ricorso a valori statici \textit{hard-coded}.
Questa distinzione consente di qualificare il sistema come \emph{data-authentic}: ogni informazione è giustificata, tracciabile e contestualizzata rispetto al suo ruolo operativo.

In sintesi, il Layer~7 completa l’architettura di \textsc{PENTION-M} rendendo esportabile l’intero processo decisionale: non soltanto i risultati finali, ma anche il \emph{contesto} e le \emph{prove di integrità} necessarie a sostenere un’analisi forense verificabile e una revisione indipendente.

\section{Discussione architetturale e trade-off}
\label{sec:arch_tradeoff}

La progettazione di \textsc{PENTION-M} ha richiesto una serie di scelte architetturali e modellistiche che implicano inevitabili \emph{trade-off}.
Tali scelte non sono riconducibili a un’unica dimensione di ottimizzazione, ma riflettono il tentativo di bilanciare esigenze spesso contrastanti: fedeltà fisica, accuratezza predittiva, robustezza operativa, auditabilità forense e riproducibilità.

Questa sezione discute criticamente i principali \textit{trade-off} emersi durante la progettazione e l’implementazione dell’artefatto, contestualizzandoli rispetto agli obiettivi del progetto e ai requisiti espressi dagli stakeholder.

\subsection{Fisica vs fedeltà del modello}

Un primo \textit{trade-off} fondamentale riguarda il livello di fedeltà fisica della simulazione della dispersione.
L’utilizzo di un modello di tipo \emph{Gaussian Puff} come \textit{baseline} fisica comporta una semplificazione significativa dei fenomeni reali di trasporto e diffusione in ambiente urbano.
Come discusso nel Layer~3, la scelta del modello Gaussian Puff privilegia controllabilità e interpretabilità rispetto alla fedeltà aerodinamica fine.

Questa scelta è deliberata: l’obiettivo del modello fisico non è riprodurre con elevata accuratezza la dinamica atmosferica reale, bensì fornire una rappresentazione controllabile, interpretabile e coerente con le principali leggi fisiche.
L’adozione di un modello fisico relativamente semplice consente:
(i) di mantenere trasparenza sulle assunzioni effettuate;
(ii) di preservare stabilità numerica e riproducibilità;
(iii) di disporre di una \textit{baseline} interpretabile per l’integrazione con modelli PIML.

La perdita di fedeltà fisica viene compensata dall’introduzione di un livello di correzione basato su \emph{Physics-Informed Machine Learning}, che apprende deviazioni sistematiche rispetto alla simulazione semplificata.
In questo senso, \textsc{PENTION-M} non persegue la massima accuratezza fisica in un singolo modello, ma una combinazione modulare di fisica esplicita e apprendimento guidato dai dati.

\subsection{PIML vs approcci puramente data-driven}

Un secondo \textit{trade-off} rilevante riguarda la scelta di adottare modelli \emph{Physics-Informed} invece di approcci puramente \textit{data-driven}.
I modelli \textit{data-driven} offrono in genere un’elevata capacità di adattamento ai dati di training, ma soffrono di problemi di interpretabilità, generalizzazione e stabilità in contesti fisico-operativi complessi.

Nel contesto di \textsc{PENTION-M}, tali limiti risultano particolarmente critici: il sistema opera in un dominio \textit{cyber-fisico}, in cui le predizioni devono essere compatibili con vincoli ambientali e difendibili in sede di audit.
L’integrazione di \textit{feature} fisiche esplicite, di vincoli nella funzione di \textit{loss} e di maschere urbane consente ai modelli PIML di ridurre lo spazio delle soluzioni ammissibili, favorendo predizioni fisicamente plausibili.

Il \textit{trade-off} consiste in una maggiore complessità progettuale e in una fase di training più articolata, a fronte di una maggiore robustezza e interpretabilità delle inferenze.
Nel contesto applicativo considerato, questa scelta risulta coerente con i requisiti di affidabilità e tracciabilità del sistema.

\subsection{Accuratezza vs robustezza operativa}

Un ulteriore \textit{trade-off} riguarda il rapporto tra accuratezza numerica e robustezza operativa.
In \textsc{PENTION-M}, l’accuratezza delle predizioni non è l’unico criterio di qualità: la stabilità delle stime, la coerenza tra modelli, la confidenza associata agli output e il comportamento sotto rumore rivestono un ruolo altrettanto rilevante.

L’introduzione di meccanismi di monitoring \textit{runtime}, di \textit{drift} fisicamente informato e di metriche di stabilità riflette una visione sistemica della qualità del modello.
Piuttosto che massimizzare una singola metrica offline, l’architettura privilegia la capacità del sistema di:
(i) segnalare condizioni anomale;
(ii) degradare in modo controllato;
(iii) mantenere tracciabilità delle decisioni.

Questo approccio può portare a sacrificare marginalmente l’accuratezza puntuale in alcuni scenari, ma aumenta significativamente l’affidabilità complessiva in un contesto operativo e forense.

\subsection{Simulazione vs dati reali}

Infine, un \textit{trade-off} strutturale riguarda l’utilizzo di dati simulati in luogo di dati reali.
L’intero sistema opera in un contesto simulato, che consente un controllo completo sulla \emph{ground truth}, sulla variabilità degli scenari e sulla riproducibilità degli esperimenti.

La simulazione introduce inevitabilmente una distanza rispetto alle condizioni operative reali, ma offre vantaggi cruciali in fase di ricerca e progettazione:
(i) esplorazione sistematica di scenari estremi;
(ii) valutazione controllata dei modelli;
(iii) possibilità di validazione \textit{by-construction}.

L’architettura di \textsc{PENTION-M} è progettata in modo tale che i confini tra simulazione e possibile \textit{deployment} reale siano chiaramente identificabili.
I layer di ingestion, inferenza, monitoring e logging non dipendono intrinsecamente dalla natura simulata dei dati, rendendo il sistema concettualmente estendibile a contesti reali con minime modifiche infrastrutturali.

\section{Collegamento al Capitolo di Valutazione}
\label{sec:arch_to_evaluation}

Il presente capitolo ha descritto in modo dettagliato l’architettura e la realizzazione di \textsc{PENTION-M}, mettendo in evidenza le scelte progettuali, i meccanismi di integrazione fisica e \textit{data-driven} e le soluzioni adottate per garantire tracciabilità e auditabilità del sistema.

Il Capitolo~\ref{chap:evaluation} si colloca come naturale prosecuzione di questa analisi architetturale.
La valutazione dell’artefatto non viene infatti condotta come un’analisi isolata delle prestazioni di singoli modelli, ma come una verifica sistematica delle proprietà emerse \emph{by construction} lungo l’intero processo di progettazione.

In particolare, il capitolo di valutazione analizzerà:
(i) la stabilità e l’affidabilità dei modelli PIML;
(ii) il comportamento del sistema in termini di monitoring e \textit{drift};
(iii) la coerenza delle predizioni rispetto ai vincoli fisici;
(iv) la robustezza dei meccanismi di logging e verifica forense.

In questo modo, la valutazione non si limita a misurare risultati, ma verifica la coerenza tra obiettivi dichiarati, scelte architetturali e comportamento osservato del sistema, chiudendo il ciclo metodologico della \emph{Design Science Research} adottata per lo sviluppo di \textsc{PENTION-M}.